# C4 — fixed one-value and table outputs match §14.1

## Automated evidence

`human_command_transcripts` snapshots `which`, `tasks`, `config --origin`, and
`locks` from the built binary. It checks that empty config lists and maps both
render visibly as `-`, no row carries trailing whitespace, and `which` remains
one raw executable path.

## Demonstration

Retained capture: `captures/C4.txt`. The observed config table and `which`
value are quoted verbatim:

```text
Effective config for repo/work
  key          scope  layer   value
  adapters     .      merged  -
  bin          .      merged  bin
  copy         .      merged  -
  detect       .      merged  depth, ignore
  dirs         .      merged  -
  env          .      merged  -
  files        .      merged  -
  ports        .      merged  -
  seed         .      merged  -
  sync_inputs  .      merged  README.md
  task         .      merged  build, fmt, hello, lint, service, sync, test
/usr/bin/git
```

Regenerate it exactly with:

```sh
env WT_PROOF_CAPTURE=C4 cargo test --quiet --test human_transcripts human_command_transcripts -- --exact --nocapture > .handoff/session-ux/captures/C4.txt 2>&1
```

## Gap

The fixture has only root-scoped repository tasks and free locks. It does not
exercise nested scopes, override collisions, or a live lock holder; those richer
rows remain outside this capture.
