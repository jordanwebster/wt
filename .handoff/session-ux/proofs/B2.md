# B2 — the live session carries the tree environment

## Automated evidence

`new_attaches_and_the_live_pane_has_the_tree_environment` types a command into
the live private tmux pane, polls `capture-pane -p`, and asserts the exact linked
tree binary, `$WT_ROOT`, and `$WT_PORT_HTTP`. The retained pane snapshot is also
checked by `insta`.

## Demonstration

Retained capture: `captures/B2.txt`. The wrapped command echo is deliberately
included; this is the complete nonblank pane capture, quoted verbatim:

```text
--- B2 ---
TP"; printf '__TREE_ENV__\n'; printf 'ROOT=%s PORT=%s\n' "$WT_ROOT" "$WT_PORT_HT
<ROOT>/wt-home/trees/repo/work/bin/tree-tool
ROOT=<ROOT>/wt-home/trees/repo/work PORT=20016
__TREE_ENV__
sh-3.2$
```

Regenerate it exactly with:

```sh
env WT_PROOF_CAPTURE=B2 cargo test --quiet --test session_flow new_attaches_and_the_live_pane_has_the_tree_environment -- --exact --nocapture > .handoff/session-ux/captures/B2.txt 2>&1
```

## Gap

This proves one declared bin and one named port end to end. Multiple bins and
aliases are covered by the environment contract suite, not by another live
pane matrix.
