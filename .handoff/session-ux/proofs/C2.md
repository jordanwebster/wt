# C2 — JSON schema, field sets, and ordering are unchanged

## Automated evidence

`every_verb_has_a_normalized_json_contract_snapshot` invokes every
report-producing verb with `--json`, normalises only the nondeterministic fields
declared by SPEC §14.5, and compares the complete canonical envelope with the
committed snapshots. No schema, field set, or ordering changed. Six envelope
values changed because behavior changed between the implementation base
`a6790c3` and reviewed head `946f363`:

- `close`: `closed` changes from false to true because a real session exists.
- `env`: the effective environment gains the tmux terminal value.
- `new`: `session` changes from absent to present.
- `open`: the former unsupported refusal becomes a successful session report.
- `register`: the backend-selection notice is present.
- `remove`: `session_closed` changes from false to true.

## Demonstration

Retained capture: `captures/C2.txt`. Its complete changed-snapshot list is
quoted verbatim:

```text
tests/snapshots/json_snapshots__close.snap
tests/snapshots/json_snapshots__env.snap
tests/snapshots/json_snapshots__new.snap
tests/snapshots/json_snapshots__open.snap
tests/snapshots/json_snapshots__register.snap
tests/snapshots/json_snapshots__remove.snap
```

Regenerate the capture exactly with:

```sh
cargo test --quiet --test json_snapshots every_verb_has_a_normalized_json_contract_snapshot -- --exact --nocapture > .handoff/session-ux/captures/C2.txt 2>&1; git diff --name-only a6790c3..946f363 -- 'tests/snapshots/json_snapshots__*.snap' >> .handoff/session-ux/captures/C2.txt
```

## Gap

Snapshot comparison proves repository-known envelopes, not compatibility with
an unknown consumer of undocumented whitespace. The six-file comparison is a
historical comparison to the reviewed head; subsequent fixes in this pass can
change values inside those same files without changing that field-set claim.
