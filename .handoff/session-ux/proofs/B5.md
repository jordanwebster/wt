# B5 — agent flag belongs to open

## Automated evidence

`new_rejects_agent_while_open_agent_starts_the_requested_recipe` observes clap
rejecting `wt new ... --agent probe` with exit 2 and no session, then observes
`wt open --agent probe --no-attach` create a private session whose recording
agent emits exactly one `start`.

## Demonstration

Captured CLI and agent output:

```text
error: unexpected argument '--agent' found
__AGENT_start__
```

Replay: `cargo test --test session_flow new_rejects_agent_while_open_agent_starts_the_requested_recipe -- --exact --nocapture`

## Gap

Gap: shell-completion text is generated from the same clap model but is not
golden-tested for the removed flag independently.
