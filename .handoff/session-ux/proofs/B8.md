# B8 — settings and one-time backend resolution

## Automated evidence

`register_declares_the_session_backend_once_and_legacy_agent_setting_is_rejected`
observes registration write `backend = "tmux"`, leave the agent key absent,
avoid a second `-V` probe, choose `"none"` for a 3.1 shim, and reject the
removed setting with a `session.agent` remedy. Core tests also assert defaults,
validation, and insertion into an existing session table.

## Demonstration

Captured fresh configuration:

```toml
[session]
backend = "tmux"
```

Captured legacy remedy:

```text
wt: SETTINGS_INVALID — `default_agent` is no longer a valid setting
remedy: move the agent name to `session.agent` in `$WT_HOME/config.toml`
```

Replay: `cargo test --test contracts register_declares_the_session_backend_once_and_legacy_agent_setting_is_rejected -- --exact --nocapture`

## Gap

Gap: the backend-selection test uses deterministic version shims; CI's private
tmux tier separately proves the selected real backend works on Linux and
macOS.
