# B8 — settings and one-time backend resolution

## Automated evidence

`register_declares_the_session_backend_once_and_legacy_agent_setting_is_rejected`
observes tmux 3.4 selection, no default agent, no second `-V`, `none` for a 3.1
shim, the doctor backend finding, and the legacy-setting remedy.
`session_verbs_resolve_a_backend_for_preexisting_homes_once` removes the config
from a registered home, then proves first-use resolution writes and reports the
choice once. Core and integration tests cover `[session]` insertion and an
actionable refusal for an inline table.

## Demonstration

Retained capture: `captures/B8.txt`. Verbatim observed block:

```text
--- B8 ---
selected: tmux 3.4
config: backend = "tmux"
second -V probes: 0
unavailable selection: none
session.agent present by default: false
```

Regenerate it exactly with:

```sh
env WT_PROOF_CAPTURE=B8 cargo test --quiet --test contracts register_declares_the_session_backend_once_and_legacy_agent_setting_is_rejected -- --exact --nocapture > .handoff/session-ux/captures/B8.txt 2>&1
```

## Gap

The “no second `-V`” assertion reads an absent log with `unwrap_or_default` and
has no positive control for that replacement shim. The absent agent key is a
small fresh-file check; agent validation itself is covered by core settings
tests. Real tmux execution is proven separately by the private-server tier.
