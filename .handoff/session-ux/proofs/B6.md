# B6 — open all covers live trees and resumes

## Automated evidence

`agents_start_only_for_new_sessions_and_open_all_resumes_recorded_agents`
creates three live trees in the registry, closes the one with recorded agent
intent, and runs `open --all`. It asserts three session reports, no attachment,
one `resume` for the recorded tree, and `start` for each unrecorded tree under
the configured session agent.

## Demonstration

Captured session result and recorder summary:

```text
repo           created   start
repo/one       created   resume
repo/two       created   start
attached clients: 0
```

Replay: `cargo test --test session_flow agents_start_only_for_new_sessions_and_open_all_resumes_recorded_agents -- --exact --nocapture`

## Gap

Gap: all three fixtures are ready directories; recovery phases that cannot
enter a door remain governed by the lifecycle refusal rather than this claim.
