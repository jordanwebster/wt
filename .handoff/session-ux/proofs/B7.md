# B7 — backend none means no tmux

## Automated evidence

`backend_none_never_invokes_tmux_for_truth_or_teardown` puts a recording shim
named `tmux` first on PATH after registration, then runs `list`, `remove`, and
`prune` and asserts the recording file does not exist. It also observes
`open`/`close` exit 5 with `session.backend` and `backend = "tmux"` in the
human error, still with zero shim invocations.

## Demonstration

Captured refusal:

```text
wt: SESSION_DISABLED — `session.backend` is `none`
remedy: set `session.backend = "tmux"` in `$WT_HOME/config.toml`
tmux invocation log: absent
```

Replay: `cargo test --test session_flow backend_none_never_invokes_tmux_for_truth_or_teardown -- --exact --nocapture`

## Gap

Gap: B7 names `list`, `remove`, and `prune`; other diagnostic commands are
covered by the declared-backend implementation but are not part of the
recording-shim assertion.
