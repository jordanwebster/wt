# D3 — stale `new --agent` and `default_agent` guidance is absent

## Automated evidence

The source-and-doc search finds no `wt new ... --agent` syntax. The only
remaining `default_agent` references are the parser’s deliberate rejection and
tests that assert the legacy key is rejected with a migration remedy.

## Demonstration

Retained capture: `captures/D3.txt`. Its complete output is quoted verbatim:

```text
tests/contracts.rs:369:    common::write(&legacy.home.join("config.toml"), "default_agent='codex'\n");
crates/wt-core/src/settings.rs:203:    if value.contains_key("default_agent") {
crates/wt-core/src/settings.rs:207:            "`default_agent` is no longer a valid setting",
crates/wt-core/src/settings.rs:437:        let legacy = parse("default_agent='missing'").unwrap_err();
```

Regenerate it exactly with:

```sh
rg -n 'default_agent|wt new[^[:cntrl:]]*--agent|--agent[^[:cntrl:]]*wt new|new .*\[--agent' README.md spec docs src crates tests > .handoff/session-ux/captures/D3.txt
```

## Gap

This is a text search, not a semantic documentation parser. It intentionally
excludes historical handoff briefs and captures, which preserve superseded
requests rather than user-facing guidance.
